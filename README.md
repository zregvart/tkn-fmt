# Opinionated formatter for Tekton Task YAML files

To build run `go build`, to install put the `tkn-fmt` in your `TKN_PLUGINS_DIR`,
`$HOME/.config/tkn/plugins` or `$XDG_CONFIG_HOME/tkn/plugins`.

The binary can be downloaded from the [releases](releases) page.
